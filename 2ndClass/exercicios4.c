#include <stdio.h>
#include <stdlib.h>

int main (){
    int evento = 5;
    char corrida = 'C';
    float tempo = 27.25;

    printf("\nTempo vitorioso: %c", corrida);
  
    printf("\nCompetição: %d : %.2f", evento, tempo);

    return 0;
}