/*
Diversos elementos cruciais estão ausentes no código abaixo. 
As diretivas #include, a especificação INT da função main, 
além de o return 0 e outros detalhes importantes. 

As variáveis estão separadas incorretamente, 
assim como os aspas necessários no printf estão ausentes.


main()
{
int a=1; b=2; c=3:
printf("0s números são: %d%d%d\n, a, b, c, d);
system("pause");
}

Segue o código corrigido.

*/
#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a=1, b=2, c=3;

    printf("0s números são: %d, %d, e %d\n", a, b, c);
    system("pause");

return 0;

}
